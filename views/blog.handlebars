<div class="blog-details">
    <h1>{{title}}</h1>
    <h3>{{minor_description}}</h3>
    <img src="{{img_link}}" alt="Blog Image">
    <p>{{content}}</p>
    <a href="/blogs" class="btn">Back to Blogs</a>
</div>